spring:
  datasource:
    url: jdbc:h2:mem:test
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

javax:
  persistence:
    schema-generation:
      database:
        action: create
        create-source: metadata
